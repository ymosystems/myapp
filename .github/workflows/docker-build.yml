name: Build and Push Docker Image

on:
  push:
    branches:
      - main  # запускать при пуше в main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.Kvadratnie Teni }}
          password: ${{ secrets.dckr_pat_epiMqs4Ic-GUfccSoXGczlnOhQ8 }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ymosystems/myapp:latest
